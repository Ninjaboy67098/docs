# Password required to view the documentation
DOCS_PASSWORD=your_password_here

# Optional: port for the auth gateway (default: 8080)
# AUTH_PORT=8080

# Optional: URL of the docs server when using dev:protected (default: http://localhost:3000)
# DOCS_TARGET=http://localhost:3000

# Optional: secret for signing the auth cookie (change in production)
# COOKIE_SECRET=your-random-secret
